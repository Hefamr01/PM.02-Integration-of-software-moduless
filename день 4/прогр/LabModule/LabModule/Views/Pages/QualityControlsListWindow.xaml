<Window
    x:Class="LabModule.Views.Pages.QualityControlsListWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="История контроля"
    Width="800"
    Height="500"
    WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DataGrid
            AutoGenerateColumns="True"
            IsReadOnly="True"
            ItemsSource="{Binding Controls}"
            SelectedItem="{Binding SelectedControl}" />
        <StackPanel
            Grid.Row="1"
            Margin="0,10,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <ComboBox
                x:Name="DecisionCombo"
                Width="120"
                Margin="5"
                SelectedIndex="0">
                <ComboBoxItem Content="approved" />
                <ComboBoxItem Content="blocked" />
            </ComboBox>
            <TextBox
                x:Name="CommentBox"
                Width="200"
                Margin="5"
                ToolTip="Комментарий (обязателен для блокировки)" />
            <Button
                Width="120"
                Margin="5"
                Command="{Binding TakeDecisionCommand}"
                CommandParameter="{Binding SelectedItem.Content, ElementName=DecisionCombo}"
                Content="Принять решение" />
        </StackPanel>
    </Grid>
</Window>