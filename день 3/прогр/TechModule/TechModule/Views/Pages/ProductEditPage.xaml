<Page
    x:Class="TechModule.Views.Pages.ProductEditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Редактирование продукта">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Margin="0,5" Text="Название:" />
        <TextBox
            Grid.Row="1"
            Margin="0,0,0,10"
            Text="{Binding Product.name}" />

        <TextBlock
            Grid.Row="2"
            Margin="0,5"
            Text="Тип:" />
        <TextBox
            Grid.Row="3"
            Margin="0,0,0,10"
            Text="{Binding Product.type}" />

        <TextBlock
            Grid.Row="4"
            Margin="0,5"
            Text="Форма выпуска:" />
        <TextBox
            Grid.Row="5"
            Margin="0,0,0,10"
            Text="{Binding Product.form}" />

        <TextBlock
            Grid.Row="6"
            Margin="0,5"
            Text="Статус:" />
        <TextBox
            Grid.Row="7"
            Margin="0,0,0,10"
            Text="{Binding Product.status}" />

        <StackPanel
            Grid.Row="8"
            Margin="0,10"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="100"
                Margin="5"
                Command="{Binding SaveCommand}"
                Content="Сохранить" />
            <Button
                Width="100"
                Margin="5"
                Click="Cancel_Click"
                Content="Отмена" />
        </StackPanel>
        <TextBlock
            Grid.Row="8"
            Margin="5"
            VerticalAlignment="Center"
            Foreground="Red"
            Text="{Binding Error}" />
    </Grid>
</Page>